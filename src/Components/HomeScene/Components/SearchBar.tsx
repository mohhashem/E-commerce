import React from "react";
import SearchIcon from "@material-ui/icons/Search";

const Searchbar = () => {
  return (
    <div className="searchBar-wrap">
      <input type="text" color="red" placeholder="Ecommmerce Search" />
    </div>
  );
};

export default Searchbar;
